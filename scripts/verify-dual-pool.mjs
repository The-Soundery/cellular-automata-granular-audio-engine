/**
 * V2 dual-pool / structure scoring removed in V3.
 * Kept as a stub so old npm habits don't fail mysteriously.
 */
console.log(
  "verify-dual-pool: skipped — V3 has no sustain/chaos voice pools (see Creative Brief v3.txt)",
);
