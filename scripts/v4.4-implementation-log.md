# V4.4 Implementation Log

**Status: CODE COMPLETE — harness-green 2026-08-10/11.** Owner listening
re-pass outstanding. Do not re-execute the work order.

**Start here:** `Implementation Filter.txt` → this file →
`scripts/listening-gate-sonic-laws.md` (unchecked items are the ear pass).
North star: `Creative Brief v4.txt`. Work order (historical):
`IMPLEMENTATION_PLAN_V4.4.md`.

Branch: `v4.1` (pushed with V4.4 commits). Verify: `npm run build && npm run verify`.

---

## Agent handoff — continue listening / QA elsewhere

### What is done (code + docs)

All eight plan phases landed, one commit per phase (plus Phase 2f saturate and
Phase 3 sign-off history). Automated gate green:

```
Phase 0–4 verify: passed
All stage ≤ 9 assertions passed.
All stage ≤ 7 assertions passed.
```

| Phase | Commit (subject) | Result |
|-------|------------------|--------|
| 1 | Fix pattern canvas staying visible over live Utomata | CSS `[hidden]` wins; Gate 1 display=`none` |
| 2 | Shared region anchor for spawn + pan/Y follow | bg pan Δ=0; bar tracks |
| 2f | Saturate pan at torus seam | separate revertible commit (D4) |
| 3 | SPAWN_SPREAD_MIN 0.5 + per-slot stasis assert | moving-bar **excluded** from slot assert (owner 2026-08-10) |
| 4 | Osc whole-share burst + OSC_DUTY 0.35 | also fixed share-guard double-count |
| 5 | Six listening sims | no new verify asserts except osc-field pulse-mod |
| 6 | Regime overlay + legend | ImageData layer under calm silhouettes |
| 7 | Per-pool δ̄ / chaos t meters | 5 Hz EMA on Field observation panel |
| 8 | Filter / Brief / listening-gate docs | OPEN ISSUE 1 CLOSED; defects → unchecked |

### What the owner / next agent must do (ears + UI)

Checklist lives in `scripts/listening-gate-sonic-laws.md` — items under
**Re-listen after V4.4 fixes** and other `[ ]` lines. Do **not** check them on
the owner's behalf.

**Explicit Gate leftovers from the plan:**

1. **Gate 3(iv)** — listen to `uniform-static` and `hue-drift`; confirm pan and
   spectral width opened up (sd ~29/32 cells after Phase 3).
2. **Gate 4(iv)** — listen to `blinker-slow`; confirm a discrete pulse (not a
   smeared gated tone). Envelope fractions 0.04/0.2 intentionally unchanged.
3. **Gate 6** — overlay screenshots / frame-time on `half-half` and
   `chaos-blob-10pct` were **not** captured (browser MCP dropped). Spot-check
   regime colours + legend visually.
4. **Gate 7** — live Utomata: after ~8 s settle, Chaos δ̄ should fall in
   **0.26–0.30** and Chaos t in **0.75–0.87** (panel meters, or
   `window.__fieldObserver.observation`).
5. Full unchecked listening-gate pass (identity sims, chaos-blob density, etc.).

### Measured findings (not failures — report, don't "fix")

**Spawn dispersion (Gate 3ii/iii)** after `SPAWN_SPREAD_MIN=0.5`:

| sim | sd(x)/sd(y) | note |
|-----|-------------|------|
| uniform-static | 28.7 / 31.6 | was ~4.6 / 5.1 |
| hue-drift | 29.2 / 32.0 | was ~4.7 / 5.2 |
| frozen-noise | 37.8 / 36.5 | unchanged |
| gradient-static | ~37.5 / 31 | unchanged |
| slot stab | uniform/breathing 0.0000; glider-swarm 0.0079 | bound 0.01 |

If **glider-swarm** slot pan sd flakes above 0.01: owner ruled that edge-snap
near movers is acceptable (same class as moving-bar exclusion). **Report only;
do not widen the bound.**

**Osc (Gate 4ii/iii)** after Phase 4:

| sim | mean osc | share | peak | mean dur | pulse fold (all / osc-only) |
|-----|----------|-------|------|----------|------------------------------|
| blinker-fast | ~3.0 | 6 | 6 | 30 ms | ~0.3–0.4 dB / ~22 dB |
| blinker-slow | ~3.0 | 6 | 6 | 70 ms | ~0.4–0.6 dB / ~210 dB |
| osc-field | ~32 | 64 | 64 | 30 ms | ~21 dB / ~21 dB |

bursts/s still 15.1 / 5.1 (stage 6 bands untouched).

**Latent bug fixed in Phase 4:** the osc share guard counted each new grain in
both `active` and `events`, capping bursts at `share/2` (hid behind
`oscBurstMax=3`). Snapshot `oscActiveBefore` like calm packing. Keep both
budget and share guards.

**New sims (Gate 5 table, approximate):**

| sim | areas / shares | notes |
|-----|----------------|-------|
| chaos-blob-2pct | chaos ~2.4%, share ~2, ~45 ev/s | prediction was share 1 / ~16/s; edge chaos fattens bag |
| chaos-blob-10pct | chaos ~10.7%, share ~7, ~191 ev/s | prediction share 6 / ~96/s |
| hue-bands | 3 calm regions | adjacent RGB dist ~0.075 < `regionColourEps` 0.12 → merge |
| pulse-calm | periodSec **1.0**, confidence **~0.48** | need ≥0.72 unmet: `rhythmHistory=48` ≈1.6 cycles; large jumps wipe calm clock |
| identity-quadrants | 3 regions, matched luminance | listening instrument |
| osc-field | 100% osc, share 64 | extreme Phase 4 coverage |

**Pan seam (D4):** saturate implemented in its own commit; owner may prefer wrap
— revert that commit alone if so.

### STOP / do not touch

See Filter STOP list. Especially: no Q←Y-extent (Deferred §10a), no blue-noise
spawn, no fourth regime for movers, do not lower `deltaRateNorm`, do not
harmonise osc envelope 0.04/0.2, do not weaken assertions (R1).

### How to run

```
npm run build && npm run verify
npm run dev
```

Console probe for live observation (audio optional):

```js
(()=>{ const o = window.__fieldObserver.observation;
  return { calm:o.calmAreaFraction, static:o.texturedAreaFraction,
    chaos:o.chaosAreaFraction, chaosMeanDelta:o.chaotic.meanDelta,
    t: Math.min(1, o.chaotic.meanDelta/0.35),
    regions:o.coherent.length,
    osc:o.oscillators.map(g=>({period:g.period, area:g.area})) }; })()
```

---

## Phase gate notes (abbreviated)

### Gate 0
Baseline probe reproduced section 1 within stated tolerances; verify green.

### Gate 1
`getComputedStyle(.pattern-preview).display` → `"none"` after flicker → Utomata.

### Gate 2
moving-bar background pan Δ = 0.00; bar pan Δ ≈ 0.0157/step. Verify green.

### Gate 3
moving-bar dropped from `checkSlotStable` (owner amendment in plan §4b and
`scripts/render-scenarios.mjs`). Verify green.

### Gate 4–8
As table above; final verify green after Phase 8 docs.

---

## Commits on `v4.1` (V4.4 stack)

```
59d007b Correct Filter, Brief and listening gate for V4.4.
72ecd0e Surface per-pool δ̄ and chaos t on the field meters.
40b28c9 Draw textured, chaos and oscillator regimes under calm silhouettes.
dac5ced Add six listening-instrument sims for unblocked gate items.
75ba94f Spend the oscillator share as one pulsed hit with real silence.
ad40b08 Widen calm spawn dispersion and assert per-slot stasis instead of range.
9cdc83e Saturate pan at the torus seam instead of wrapping.
ae50b98 Use a shared region anchor for spawn placement and pan/Y follow.
00b86f4 Fix pattern canvas staying visible over live Utomata.
```

(Plus earlier on branch: plan + D1 sign-off commits before execution.)
