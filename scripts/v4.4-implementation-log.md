# V4.4 Implementation Log

**Status: COMPLETE** — harness-green 2026-08-10/11; owner listening signed off
2026-08-11. Do not re-execute the work order.

**Start here:** `Implementation Filter.txt` → this file → Brief.
Listening checklist (closed): `scripts/listening-gate-sonic-laws.md`.
Work order (historical): `IMPLEMENTATION_PLAN_V4.4.md`.

Branch: `v4.1`. Verify: `npm run build && npm run verify`.

---

## Sign-off (2026-08-11)

Owner ear pass: **all listening-gate items passed**, including Gate leftovers
3(iv), 4(iv), 6, 7 and the V4.4 re-listen set. Filter ear-open issues 1–4
CLOSED / ADOPTED. Deferred only: fourth regime for movers (Filter 5), loose
stage-5 chaos rate band (Filter 6), and Q←Y-extent doctrine (plan §10a).

Do **not** re-implement Phases 1–8. Do not lower `deltaRateNorm`. Do not
harmonise osc envelope 0.04/0.2. Do not weaken assertions (R1).

---

## What landed (code + docs)

All eight plan phases, one commit per phase (plus Phase 2f saturate).
Automated gate green:

```
Phase 0–4 verify: passed
All stage ≤ 9 assertions passed.
All stage ≤ 7 assertions passed.
```

| Phase | Commit (subject) | Result |
|-------|------------------|--------|
| 1 | Fix pattern canvas staying visible over live Utomata | CSS `[hidden]` wins; Gate 1 display=`none` |
| 2 | Shared region anchor for spawn + pan/Y follow | bg pan Δ=0; bar tracks |
| 2f | Saturate pan at the torus seam | separate revertible commit (D4) |
| 3 | SPAWN_SPREAD_MIN 0.5 + per-slot stasis assert | moving-bar **excluded** from slot assert |
| 4 | Osc whole-share burst + OSC_DUTY 0.35 | also fixed share-guard double-count |
| 5 | Six listening sims | osc-field pulse-mod assert |
| 6 | Regime overlay + legend | ImageData under calm silhouettes |
| 7 | Per-pool δ̄ / chaos t meters | 5 Hz EMA on Field panel |
| 8 | Filter / Brief / listening-gate docs | OPEN ISSUE 1 CLOSED |

---

## Measured findings (keep; do not “fix”)

**Spawn dispersion** after `SPAWN_SPREAD_MIN=0.5`:

| sim | sd(x)/sd(y) | note |
|-----|-------------|------|
| uniform-static | 28.7 / 31.6 | was ~4.6 / 5.1 |
| hue-drift | 29.2 / 32.0 | was ~4.7 / 5.2 |
| frozen-noise | 37.8 / 36.5 | unchanged |
| gradient-static | ~37.5 / 31 | unchanged |
| slot stab | uniform/breathing 0.0000; glider-swarm 0.0079 | bound 0.01 |

Glider-swarm slot pan sd above 0.01: report only; do not widen the bound
(edge-snap near movers — same class as moving-bar exclusion).

**Osc** after Phase 4:

| sim | mean osc | share | peak | mean dur | pulse fold (all / osc-only) |
|-----|----------|-------|------|----------|------------------------------|
| blinker-fast | ~3.0 | 6 | 6 | 30 ms | ~0.3–0.4 dB / ~22 dB |
| blinker-slow | ~3.0 | 6 | 6 | 70 ms | ~0.4–0.6 dB / ~210 dB |
| osc-field | ~32 | 64 | 64 | 30 ms | ~21 dB / ~21 dB |

bursts/s still 15.1 / 5.1. Latent bug fixed: osc share guard double-counted
new grains; snapshot `oscActiveBefore` like calm packing.

**Listening sims** (approximate):

| sim | areas / shares | notes |
|-----|----------------|-------|
| chaos-blob-2pct | chaos ~2.4%, share ~2, ~45 ev/s | edge chaos fattens bag vs share-1 ideal |
| chaos-blob-10pct | chaos ~10.7%, share ~7, ~191 ev/s | denser rattle bracket |
| hue-bands | 3 calm regions | adj. RGB ~0.075 < regionColourEps 0.12 → merge |
| pulse-calm | periodSec **1.0**, confidence **~0.48** | OSC% = 0 by design (calm rhythm clock) |
| identity-quadrants | 3 regions, matched luminance | identity axes |
| osc-field | 100% osc, share 64 | extreme whole-share pulse |

**Pan seam (D4):** saturate in its own commit — revert alone if wrap preferred.

---

## STOP / do not touch

See Filter STOP list. Especially: no Q←Y-extent without a new Brief decision,
no blue-noise spawn, no fourth regime unless deliberately starting that work,
do not lower `deltaRateNorm`, do not harmonise osc envelope 0.04/0.2, do not
weaken assertions (R1).

## How to run

```
npm run build && npm run verify
npm run dev
```

Console probe:

```js
(()=>{ const o = window.__fieldObserver.observation;
  return { calm:o.calmAreaFraction, static:o.texturedAreaFraction,
    chaos:o.chaosAreaFraction, chaosMeanDelta:o.chaotic.meanDelta,
    t: Math.min(1, o.chaotic.meanDelta/0.35),
    regions:o.coherent.length,
    osc:o.oscillators.map(g=>({period:g.period, area:g.area})) }; })()
```

---

## Commits on `v4.1` (V4.4 stack)

```
59d007b Correct Filter, Brief and listening gate for V4.4.
72ecd0e Surface per-pool δ̄ and chaos t on the field meters.
40b28c9 Draw textured, chaos and oscillator regimes under calm silhouettes.
dac5ced Add six listening-instrument sims for unblocked gate items.
75ba94f Spend the oscillator share as one pulsed hit with real silence.
ad40b08 Widen calm spawn dispersion and assert per-slot stasis instead of range.
9cdc83e Saturate pan at the torus seam instead of wrapping.
ae50b98 Use a shared region anchor for spawn placement and pan/Y follow.
00b86f4 Fix pattern canvas staying visible over live Utomata.
```
