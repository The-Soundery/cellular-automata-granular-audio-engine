Cellular Automata Granular Audio Engine — Implementation Filter
Status: CURRENT. Sonic identity is closed; do not reopen mapping without a
  new listening decision.
North star: Creative Brief.txt
Verify: `npm run build && npm run verify`
Scenarios: scripts/test-scenarios.md

------------------------------------------------------------------------------
STOP — do not revive
------------------------------------------------------------------------------

- Fixed listening points / lattice “pixels”
- Equal-share or presence/luminance amplitude on fixed addresses
- Voice ownership / structure tracking / fieldOrder scoring
- Rebake-on-change or “dumb paint of frozen scrub positions”
- Mid-grain sample-window scrub chase of moving regions
  (direct pan / Y-spectrum / channel-mix follow for region-bound grains is allowed)
- V3 identity (X→sample / RGB→envelope)
- 48-bin offline spectral bank (replaced by per-grain resonant bandpass)
- 1D hue→centroid LUT as the sole material axis (superseded by polar map)
- Mono fold-down of the source as the only playback buffer (analysis may use mono)
- Doppler / velocity→grain rate (pitch offsets beyond ±1)
- Stratified spawn sites for calm/texture are sampling, not listening posts;
  they are scoped to those two pools and must never be given persistence or
  ownership. Do not apply stratification to chaos or osc.
- Using mono fold-down `0.5*(L+R)` as the primary loudness measure — equal-power
  pan makes centre grains read ~3 dB hot; primary RMS is sqrt(mean(L²+R²))
- Treating CHAOS_EVENTS_MAX_HZ as a mix control — it is a CPU rail (4000) only;
  concurrency comes from share / duration
- Softening or deleting the segmentation ≤1 dB band to hide a meter bug
- Re-asserting audio-domain Welch-mel flux ordering — at filled concurrency it
  is near-backwards; event-domain Y/Q flux is the gate
- A sixth “moving static” regime — calm already covers translating silhouettes
- Folding flow grains back into chaos share; applying tracks on raw flow.id
  (collides with calm — use FLOW_ID_BASE)
- Treating a filled translating silhouette as flow; treating 2D scatter as
  one flow; tint-glue of hues
- Lowering deltaRateNorm against the closed live-chaos under-spend claim

------------------------------------------------------------------------------
Closed — do not reopen without new evidence
------------------------------------------------------------------------------

- Live chaos under-spend: δ̄ 0.26–0.30 → t 0.75–0.87 (~1 dB). Do not lower
  `deltaRateNorm`. DATA no longer shows δ̄ / t; the claim is closed in
  verify / live listening, not on the HUD.
- Frozen-and-detailed vs still-and-uniform are different materials.
- Percussive chaos + blinker envelope: 0.04/0.2 left alone — do not “harmonise”.
- Flow is similarity travel (colour correspondence), not pack-COM. Filled
  translating silhouettes stay calm. Fifth spend pool: trailing-edge
  piece-spawn, hop-velocity conveyor (`FLOW_ID_BASE`), packing→mid duration.
  Grains die at the leading edge and respawn at the trailing edge.

------------------------------------------------------------------------------
Still deferred
------------------------------------------------------------------------------

- Stage-5 chaos rate band is loose: full-flicker lower band 1200 Hz is ~44%
  below ideal 64/0.03 ≈ 2133. Direct guard is avg active ∈ [55, 64]. Check
  concurrency before rate.
- Random (dice) Type-U at the current depth can emit greyscale fields.
  Mapping must work for grey and colour. No undo/redo.

------------------------------------------------------------------------------
Live pipeline
------------------------------------------------------------------------------

Utomata (or TestPatterns) → FrameObserver (cold-start primes both buffers)
  → FieldObserver → GrainScheduler → AudioEngine.sendEvents (+ track)
  → grain-processor (polar HSV window; stereo L/R by channelMix;
                     absolute Y bandpass 80 Hz–12 kHz; bandwidth-compensated;
                     envNorm after attack/release rescale; equal-power pan;
                     direct pan/Y/channelMix follow)

Observation: calm / textured / chaotic / oscillators / flow (remainder
  split; flow is similarity travel, punched out of chaos, not out of calm).
Scheduler: five pools (calm / texture / chaos / osc / flow); area-share
  concurrency; flow piece-spawn + FLOW_ID_BASE tracks; split material
  (timeOrder→dur/env, spectralT→Q); stratified+dispersion sites (calm/texture);
  SPAWN_ANCHOR_R_MIN; scrub clocks; CHAOS_EVENTS_MAX_HZ CPU rail only;
  HSV→polar material query; calm/texture restricted to sustained subset.
Renderer RMS (scripts/render-scenarios.mjs): primary = sqrt(mean(L²+R²));
  mono 0.5*(L+R) reported secondary only.

Presentation (locked):
- Dark grey chrome `#1c1c1c`. Colour lives in the CA; overlay/DATA may use
  regime colours only when those layers are on (calm gold, static grey,
  chaos red, osc blue, flow green). VT323 chrome, Share Tech Mono readout.
- Corner HUD. Overlay off by default: 1px vector outlines at display
  resolution, L-brackets + COM on tracked calm/osc/flow, chaos as fragments.
- Source waveform + grain ticks sit under the field (chrome), not on the sim.
- DATA fold: regime %, spend, budget, clip, step; SIM test patterns.
  Flow % is member-cell area / grid (sparse packs read as tenths of a
  percent). Overlay outlines are dilated for visibility; scheduler share
  still uses regionArea.
- Explore: eight Type-U map neighbours (±1 x/y), audio follows centre.
  Click a neighbour to travel; click centre (or Explore) to settle.
- Pause stops new CA steps and therefore new grains. Load audio arms the
  AudioContext on the file-picker click (browser gesture).

Key modules:
- src/field/FieldObserver.ts, FrameObserver.ts, GrainScheduler.ts, TestPatterns.ts
- src/audio/spectral.ts, public/grain-processor.js
- src/ui/controls.ts, RegionOverlay.ts, WaveformStrip.ts
- src/ca/typeU.ts (programAt / neighborPrograms), UtomataHost.ts

Negotiable (not doctrine):
- FIELD_OBS — chaosDeltaMin, osc_*, flow_* (binSize≈4/maxBlocks≈192/
  scrambleFrac≈0.72/stream≈22/lane≈8/confirm≈8/travel≈1.5/size≈4 keep≈3/
  solidFill≈0.88/straightness≈0.35/localDensity≈0.72/packT≈0.65/
  persist≈0.70/hop≈0.15/hopDominate/stitchHeadingCos),
  region colour eps (flowColourEps stays 0.12 — no tint join), …
- SCHED — DUR_MIN 0.03, DUR_MAX 8, ATT_MIN 0.02, ATT_MAX 0.30,
  REL_CHAOS 0.98, REL_CALM 0.34, Q_*, WINDOW_HALF_*,
  FLOW_ORDER_MIN 0.28, FLOW_ORDER_MAX 0.52, FLOW_WINDOW_T 0.45, FLOW_ID_BASE 1e6,
  FLOW_TRAVEL_DUR_BLEND 1,
  maxCalmConcurrent/textureMaxConcurrent 64, calmPackRateMin 1.0,
  CHAOS_EVENTS_MAX_HZ 4000, deltaRateNorm 0.35 (← chaos live-balance lever),
  SPAWN_SPREAD_*, SPAWN_SLOT_Z_MAX, SPAWN_SNAP_MAX_RING, SPAWN_ANCHOR_R_MIN,
  SITE_SALT_*, SCRUB_RATE_MAX, rhythm_*, oscBurstMax
- Spectral — CENTROID_HOP_SEC, analysis FFT, polar weight / regime bias
- Worklet — Q_REF, FC_REF, SPECTRAL_TILT_COMP, ENV_RAMP_MS, FILT_*, TARGET_RMS

Harnesses:
- verify-phase0.mjs — lattice path stays gone from the live tree
- verify-phase1.mjs — flow borders (dense/stream/diagonal/train +y;
  cascade / wavefront / denser cascade; solids ≠ flow; flow-dots ≠ flow)
- verify-phase2.mjs — flow spend / FLOW_ID_BASE / mid duration band
- verify-phase4.mjs — polar / stereo / absolute Y contract
- investigate-scenarios.mjs — events, shares, sampleCenter, eventFlux (Y/Q)
- render-scenarios.mjs — total-power RMS, crest, sites, block-RMS, cold-start;
  audio flux reported not asserted
