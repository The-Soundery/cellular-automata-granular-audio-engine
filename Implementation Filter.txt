Cellular Automata Granular Audio Engine — Implementation Filter (V5)
Status: V5 CODE COMPLETE — harness-green 2026-08-11; owner ears open
  (`scripts/listening-gate-v5.md`). V4.4 COMPLETE (ears signed off 2026-08-11).
North star: Creative Brief v4.txt (includes V5 addendum)
Entry for agents: this file first, then the Brief, then
  `IMPLEMENTATION_PLAN_V5.md` and `scripts/v5-implementation-log.md`.
  Historical: `scripts/v4.4-implementation-log.md`.
  Listening: `scripts/listening-gate-v5.md` (V5); V4.4 checklist closed.

>> V4.4 Phases 1–8 are committed. Do not re-execute IMPLEMENTATION_PLAN_V4.4.md.
>> Do not lower deltaRateNorm.
>> V5 polar/stereo/relative-Y is live in code; complete listening-gate-v5.md.

Historical detail: scripts/v4.3-implementation-log.md (do not re-execute).
V4.4 gate record: scripts/v4.4-implementation-log.md
Verify: `npm run build && npm run verify`
  (= phase 0–4 scripts + investigate --stage 9 + render --stage 7)
Scenarios: scripts/test-scenarios.md

------------------------------------------------------------------------------
STOP — do not revive
------------------------------------------------------------------------------

- Fixed listening points / lattice “pixels”
- Equal-share or presence/luminance amplitude on fixed addresses
- Voice ownership / structure tracking / fieldOrder scoring
- Rebake-on-change or “dumb paint of frozen scrub positions”
- Mid-grain sample-window scrub chase of moving regions
  (direct pan / Y-spectrum / channel-mix follow for region-bound grains is allowed)
- V3 identity (X→sample / RGB→envelope) without a new listening decision
- 48-bin offline spectral bank (replaced by per-grain resonant bandpass)
- 1D hue→centroid LUT as the sole material axis (superseded by V5 polar map)
- Mono fold-down of the source as the only playback buffer (analysis may use mono)
- Doppler / velocity→grain rate (pitch offsets beyond ±1) — rejected
- Stratified spawn sites for calm/texture are sampling, not listening posts;
  they are scoped to those two pools and must never be given persistence or
  ownership. Do not apply stratification to chaos or osc.
- Using mono fold-down `0.5*(L+R)` as the primary loudness measure — equal-power
  pan makes centre grains read ~3 dB hot; primary RMS is sqrt(mean(L²+R²))
- Treating CHAOS_EVENTS_MAX_HZ as a mix control — it is a CPU rail (4000) only;
  concurrency comes from share / duration
- Softening or deleting the segmentation ≤1 dB band to hide a meter bug
- Re-asserting audio-domain Welch-mel flux ordering — at filled concurrency it
  is near-backwards; event-domain Y/Q flux is the gate

Archives (reference only): public/_obsolete/ (failed lattice path — do not revive)
Superseded work orders (deleted from tree): IMPLEMENTATION_PLAN_V4.2/V4.3,
  v4.2-implementation-log, audio-system-report — history lives in git + v4.3 log

------------------------------------------------------------------------------
OPEN / DEFERRED — read before changing chaos rate, Q, or regimes
------------------------------------------------------------------------------

These are NOT harness failures and are NOT listening-gate leftovers.
Automated gates and the V4.4 ear pass are green. Do not "fix" remaining
items by fitting thresholds to saturated-δ test patterns.

CLOSED (do not reopen without new evidence):

1. Chaos under-spend on real Utomata — CLOSED (measured 2026-08-06; ear
   confirmed Gate 7 2026-08-11). Live chaos δ̄ 0.26–0.30 → t 0.75–0.87
   (~1 dB under-spend). The old “δ ≈ 0.1 / ~5 dB” premise was wrong. Do not
   lower `deltaRateNorm`. UI surfaces chaos δ̄ / t on the Field panel.

2. Frozen-and-detailed vs still-and-uniform — CLOSED (ear 2026-08-11).
   Headless mean Q frozen 1.32 < uniform 8.00; owner confirmed distinct
   materials at matched loudness (`frozen-noise` vs `uniform-static`).

3. Percussive chaos + blinker envelope — CLOSED (ear 2026-08-11).
   Sparse chaos (`chaos-blob-2pct`) reads percussive; denser coverage
   (`chaos-blob-10pct`) reads as a rattle. Blinker / osc whole-share pulses
   with silence between hits. Envelope 0.04/0.2 left alone — do not
   “harmonise”. Percussiveness is a coverage consequence (share ÷ duration).

4. Creative Brief V4.3 / V4.4 doctrine addenda — ADOPTED (owner 2026-08-11).
   Lines live in Creative Brief v4.txt (V4.3 §1–4 and V4.4 addenda).

Still deferred / harness notes:

5. Deferred: moving coherent structures (gliders as figure)
   κ = similarity × stability → movers never form regions. Needs a fourth
   regime. glider-swarm only improves via its calm background today.

6. Stage-5 chaos rate band is loose
   full-flicker lower band 1200 Hz is ~44% below ideal 64/0.03 ≈ 2133.
   Direct guard is avg active ∈ [55, 64]. Check concurrency before rate.

7. Deferred (separate from V5): Prev/Next/Random Type-U variations emit
   scalar greyscale fields. Owner pastes coloured equations; mapping must
   work for grey and colour. Do not block V5 on equation-family rewrite.

------------------------------------------------------------------------------
Live pipeline
------------------------------------------------------------------------------

Utomata (or TestPatterns) → FrameObserver (cold-start primes both buffers)
  → FieldObserver → GrainScheduler → AudioEngine.sendEvents (+ track)
  → grain-processor (polar HSV window; stereo L/R by channelMix;
                     relative Y bandpass; bandwidth-compensated;
                     envNorm after attack/release rescale; equal-power pan;
                     direct pan/Y/channelMix follow)

Observation: calm / textured / chaotic / oscillators (remainder split).
Scheduler: area-share concurrency; split material (timeOrder→dur/env,
  spectralT→Q); stratified+dispersion sites (calm/texture); SPAWN_ANCHOR_R_MIN;
  scrub clocks; CHAOS_EVENTS_MAX_HZ CPU rail only; HSV→polar material query
  with regime stationarity bias.
Renderer RMS (scripts/render-scenarios.mjs): primary = sqrt(mean(L²+R²));
  mono 0.5*(L+R) reported secondary only.

Key modules:
- src/field/FieldObserver.ts, FrameObserver.ts, GrainScheduler.ts, TestPatterns.ts
- src/audio/spectral.ts, public/grain-processor.js
- src/ui/controls.ts (field % + active/share spend meter), RegionOverlay.ts

Negotiable (not doctrine):
- FIELD_OBS — chaosDeltaMin, osc_*, region colour eps, …
- SCHED — DUR_MIN 0.03, DUR_MAX 8, ATT_MIN 0.02, ATT_MAX 0.30,
  REL_CHAOS 0.98, REL_CALM 0.34, Q_*, WINDOW_HALF_*,
  maxCalmConcurrent/textureMaxConcurrent 64, calmPackRateMin 1.0,
  CHAOS_EVENTS_MAX_HZ 4000, deltaRateNorm 0.35 (← chaos live-balance lever),
  SPAWN_SPREAD_*, SPAWN_SLOT_Z_MAX, SPAWN_SNAP_MAX_RING, SPAWN_ANCHOR_R_MIN,
  SITE_SALT_*, SCRUB_RATE_MAX, rhythm_*, oscBurstMax, Y_OCTAVE_SPAN
- Spectral — CENTROID_HOP_SEC, analysis FFT, polar weight / regime bias
- Worklet — Q_REF, FC_REF, SPECTRAL_TILT_COMP, ENV_RAMP_MS, FILT_*, TARGET_RMS

Harnesses:
- investigate-scenarios.mjs — events, shares, sampleCenter, eventFlux (Y/Q)
- render-scenarios.mjs — total-power RMS, crest, sites, block-RMS, cold-start;
  audio flux reported not asserted

------------------------------------------------------------------------------
Adopted Brief doctrine (V4.3 + V4.4 + V5)
------------------------------------------------------------------------------

- Neutrality is measured, not assumed. Any mapping axis that changes output
  energy is a bug until proven flat. The engine compensates filter bandwidth;
  it does not compensate the source spectrum.
- Area share is spent, not merely allocated. Caps that prevent a pool from
  reaching its share are loudness policy in disguise.
- Two axes, two consequences: timeOrder → duration/envelope; similarity → Q.
  That is what makes still-and-uniform ≠ still-and-detailed at matched level.
- Sampling must not manufacture change. Stratified sites are re-derived every
  frame from the measured area — not listening posts, never owned by a grain.
- Percussiveness is a coverage consequence (share ÷ duration), not an
  envelope tweak. A pulsed pool spends its share at the pulse; silence
  between hits is not an unspent share.
- V5: HSV polar material + stereo channel mix + relative Y. Soft regime
  stationarity bias is identity, not share policy. Window stays frozen;
  channel mix may follow with pan.
